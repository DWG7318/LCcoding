# Method Architecture

LCCoding is an Owner-led, AI-executed method. Calabash continuously carries product
meaning; Workflow and UI are designed as two ends; Simulation makes both observable;
Feature Integration connects them; Verification and Owner Acceptance prove completion.

The method is iterative, not a fixed linear sequence.
