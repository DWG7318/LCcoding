{
  "identifier": "main",
  "description": "Allows the main BI window to read and set only its own native topmost state.",
  "windows": ["main"],
  "permissions": ["allow-is-pinned", "allow-set-pinned"]
}
