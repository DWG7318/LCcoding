# Historical Design Records

Artifact class: HISTORICAL_DESIGN_RECORDS
Authority: NON_NORMATIVE
Use: PROVENANCE_ONLY
Validator/runtime input: FORBIDDEN
Current semantic authority: ../../SPEC.md

The `plans/` and `specs/` descendants retain their original bytes as design and implementation provenance. They may describe old proposals, versions, rejected choices, or superseded designs; descendant wording never overrides current authority, active contracts, or focused references and is never a runtime, validator, compatibility, or release input.

To adopt a historical idea, introduce it again through current authority. Do not cite a historical record as a present rule.
